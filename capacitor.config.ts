import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cristhoper_olmedo_001D',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
